# Job Tracker — Development Guide

## Структура окружений

```
~/projects/
├── job-tracker/           # PROD (main branch) — порт 8000
│   ├── data/              # Реальные данные
│   ├── cache/             # Кэш
│   └── venv/              # Python virtualenv
│
├── job-tracker-dev/       # DEV (dev branch) — порт 8001
│   ├── data → симлинк     # Указывает на PROD/data
│   ├── cache → симлинк    # Указывает на PROD/cache
│   └── .venv → симлинк    # Указывает на PROD/venv
│
└── job-tracker-backups/   # Бэкапы данных
    └── backup_YYYYMMDD_HHMMSS/
```

## Быстрые команды (aliases)

После `source ~/.zshrc`:

| Команда | Описание |
|---------|----------|
| `jt-prod` | Запустить PROD сервер (порт 8000) |
| `jt-dev` | Запустить DEV сервер (порт 8001) |
| `jt` | Открыть PROD в браузере |
| `jt-uat` | Открыть DEV в браузере |
| `jt-backup` | Создать бэкап данных |
| `jt-merge` | Бэкап + мерж dev→main |
| `jt-restore` | Восстановить из бэкапа |

## Workflow разработки

### 1. Запуск серверов

```bash
# Терминал 1 — PROD
jt-prod

# Терминал 2 — DEV
jt-dev
```

### 2. Разработка новой фичи

```bash
# Работаем в DEV
cd ~/projects/job-tracker-dev

# Вносим изменения в код
# ...

# Коммитим
git add .
git commit -m "feat: новая фича"
git push
```

### 3. Тестирование

Открой http://localhost:8001 (DEV) и проверь что всё работает.

### 4. Мерж в PROD

```bash
# Безопасный мерж с бэкапом
jt-merge

# Или вручную:
cd ~/projects/job-tracker
./scripts/backup-data.sh
git merge dev
git push
```

### 5. Откат при проблемах

```bash
# Восстановить данные из бэкапа
jt-restore

# Откатить код
cd ~/projects/job-tracker
git revert HEAD
```

## Файлы данных

### В Git (трекаются):
- `data/companies.json` — список компаний

### НЕ в Git (runtime):
- `data/jobs_new.json` — новые вакансии
- `data/jobs_pipeline.json` — активный pipeline
- `data/jobs_archive.json` — архив
- `cache/` — кэш вакансий и статистика
- `job_status.json` — статусы заявок

## Визуальные отличия PROD/DEV

| Признак | PROD | DEV |
|---------|------|-----|
| Бейдж | 🟢 PROD (зелёный) | 🟡 DEV (оранжевый) |
| Title | Job Tracker | [DEV] Job Tracker |
| Header | Обычный | Оранжевая полоска |
| Порт | 8000 | 8001 |

## Скрипты

```
scripts/
├── backup-data.sh    # Бэкап data/, cache/, job_status.json
├── safe-merge.sh     # Бэкап + git merge dev
└── restore-data.sh   # Восстановление из бэкапа
```

## Troubleshooting

### Порт занят
```bash
# Найти процесс
lsof -i :8000  # или :8001

# Убить
kill -9 <PID>
```

### Данные потерялись после мержа
```bash
jt-restore
# Выбрать нужный бэкап
```

### Симлинки сломались (DEV)
```bash
cd ~/projects/job-tracker-dev
rm -f data cache .venv
ln -s ../job-tracker/data data
ln -s ../job-tracker/cache cache
ln -s ../job-tracker/venv .venv
```
