# Job Tracker - Продолжение разработки

## Контекст проекта

Job Tracker - персональный агрегатор вакансий для PM/TPM ролей с автоматизацией заполнения форм.

**Репозиторий:** `~/Library/Mobile Documents/com~apple~CloudDocs/Dev/AI_projects/job-tracker-dev/`

**Приложение:** http://127.0.0.1:8001/static/index.html

## Что было сделано в прошлой сессии (2026-01-28)

### 1. V6 Form Filler + AI Integration
- V6 engine успешно заполняет Greenhouse формы (тестировали Coinbase, PagerDuty, Abnormal Security)
- Добавлен AI fallback через Claude API для неизвестных вопросов
- Файлы: `browser/v6/engine.py`, `browser/v6/ai_helper.py`
- API ключ в `browser/v5/config/api_keys.json`

### 2. Multi-Machine Sync
- Исправлены hardcoded пути - теперь используется `Path.home()` для универсальности
- Добавлен daemon lock (`data/daemon.lock`) - предотвращает конфликты когда оба компа работают
- Daemon выключен по умолчанию - включать вручную только на ОДНОМ компе
- UI показывает "🔒 Daemon locked by: user@host" если занят другим компом

### 3. Текущее состояние
- Backend: FastAPI на порту 8001
- Данные синхронизируются через iCloud между компами (`anton` и `antonkondakov`)
- 3 Applied позиции: 2 Webflow + 1 TEKsystems (manual)

## Файлы для контекста

Прочитай эти файлы для понимания архитектуры:
1. `CHANGELOG_2026-01-28_session2.md` - детальный лог изменений
2. Документы проекта в Project Knowledge

## Запуск сервера

```bash
cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/Dev/AI_projects/job-tracker-dev
pkill -f "uvicorn.*8001"
python3 -m uvicorn main:app --host 0.0.0.0 --port 8001 &
open "http://127.0.0.1:8001/static/index.html"
```

## Что можно делать дальше

1. **Тестировать V6 на других ATS** (Lever, Workday)
2. **Улучшить AI** - сохранять ответы в learned database
3. **Генерация Cover Letter** - адаптировать под каждую позицию
4. **Улучшить UI** - фильтр по умолчанию, быстрые действия
5. **Pipeline workflow** - автоматизация от discovery до apply

## Важные заметки

- Username на компах разный: `anton` vs `antonkondakov`
- Пути теперь универсальные через `~/` или `Path.home()`
- Daemon lock истекает через 10 минут автоматически
- Перед включением daemon - проверяй что на другом компе он выключен
